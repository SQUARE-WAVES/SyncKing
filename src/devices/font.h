#pragma once

const uint8_t* font_start();
const size_t font_len();
const uint8_t* ascii_to_glyph(char ascii);

























































































